{
	"name": "fullstack",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"easy-start": "npm run install && npm run start",
		"test": "echo \"Error: no test specified\" && exit 1",
		"install": "(cd backend/express && npm install) && (cd frontend && npm install)",
		"start": "(cd backend/express && npm start) & (cd frontend && npm start)"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"express": "^4.17.1"
	}
}
